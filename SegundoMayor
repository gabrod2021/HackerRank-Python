if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())

    arr=list(arr)
    
mayor=max(arr)
mayor_iguales=arr.count(mayor)
for i in range(mayor_iguales):
    arr.remove(mayor)

print(max(arr))
